package studentRecordsBackupTree.bst;

/**
 * Observer interface
 */
public interface Observer {
    /**
     * Performs update of the observer
     * @param value to update with
     */
    void update(int value);
}
